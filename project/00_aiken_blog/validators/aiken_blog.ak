// test init() {
//   let x = 2

//   if x % 2 == 0 {
//     trace @"is_event"
//     True
//   } else {
//     trace @"is_odd"
//     False
//   }
// }


test init() {
    let must_be_after = True
    let must_be_before = False

    must_be_after? && must_be_before?
}